# GoCredit (Lamp Credit) logo + rename

The Anupam / lamp.credit prospect's brand is GoCredit. Set the company name to
"GoCredit" on the cached meeting and the existing `lamp-credit` room, and applied
the GoCredit logo (provided by the team) hosted at `assets/logos/gocredit.png`.
Registered `lamp.credit` in `CURATED_LOGOS` -> gocredit.png. Room slug/URL left as
`lamp-credit` so existing links keep working.
