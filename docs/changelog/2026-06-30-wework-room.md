# WeWork room (contact Arya, no notes, domain access)

Created a prospect room for WeWork directly (no Granola meeting), per request.

- Room `wework`, contact name "Arya", recap/notes section hidden
  (`hidden_sections = ["recap_discussed", "recap_next_steps"]`), empty brief.
- All standard child rows seeded (overview sub-tabs, pricing, default customer
  references + case studies).
- Logo from the official WeWork App Store icon (WeWork Companies, Inc.),
  hosted at `assets/logos/wework.png`; registered `wework.com` in
  `CURATED_LOGOS`. Brand primary left default (the logo is black/white, no
  distinct hue).
- Restricted to `@wework.com` (domain access) plus @linkrunner.io.
