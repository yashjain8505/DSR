# DailyRounds room (no Granola, no recap)

Created a prospect room for DailyRounds directly (no Granola meeting), per
request, with the recap/notes section hidden.

- Room `dailyrounds`, recap section hidden (`hidden_sections = ["recap_discussed",
  "recap_next_steps"]`), empty brief, all standard child rows seeded (overview,
  pricing, default customer references + case studies).
- Logo built from the DailyRounds wordmark provided, padded to a 512x512 brand-red
  square, hosted at `assets/logos/dailyrounds.png`. Brand primary #9a0000.
- Registered `dailyrounds.org` in `CURATED_LOGOS`.
- Access left open (no contact/domain specified); can be domain-restricted later.
